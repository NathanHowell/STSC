from distutils.core import setup

setup(
    name='stsc',
    version='0.1dev',
    packages=['stsc',],
    license='Public Domain',
    zip_safe=True,
    long_description=open('README.md').read(),
)
